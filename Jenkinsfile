pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Wab dab dub la la"
                    ls -lah
                '''
            }
        }
    }
}
